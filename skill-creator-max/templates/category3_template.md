---
title: コード生成・変換系スキルテンプレート
category: template
version: "1.0.0"
description: コード生成、リファクタリング、マイグレーション、変換用スキルのテンプレート
project: ffmpeg-command-generate-webapp
tags:
  - template
  - codegen
  - refactoring
  - migration
last_updated: "2025-02-20"
usage: >
  コード生成・変換系のスキルを新規作成する際に使用する。
  このプロジェクト固有のルール（oklch CSS, Svelte 5 Runes, i18n）を
  テンプレートに組み込み済み。YAML フロントマターは削除すること。
examples: []
---

# {{SKILL_NAME}}

{{入力}} から {{出力}} を生成（変換）する。

## 前提

- {{必要なファイル・ディレクトリ}}
- {{依存するツール・ライブラリ}}

## 入出力仕様

| 項目 | 値 |
|------|------|
| 入力 | {{入力の形式・場所}} |
| 出力 | {{出力の形式・場所}} |
| 上書き | {{既存ファイルの上書き有無}} |

## 手順

### 1. 入力の確認

{{入力ファイルの存在確認・内容検証}}

```bash
{{確認コマンド}}
```

### 2. 生成（変換）

{{生成・変換の具体的な手順}}

### 3. プロジェクトルールの適用

生成したコードが以下のルールに準拠していることを確認:

| ルール | チェック方法 |
|--------|------------|
| CSS: oklch のみ | `bash scripts/lint-css.sh` |
| Svelte 5: Runes API | `$state()`, `$derived()` 使用を確認 |
| i18n: 両言語同期 | `ja.json`, `en.json` の両方に翻訳キー追加 |
| SSR 安全: onMount 内 | `window`/`document` が `onMount` 内にあることを確認 |
| コピー: JSON方式 | `structuredClone` ではなく `JSON.parse(JSON.stringify(...))` |

### 4. バリデーション

```bash
npm run check   # 型チェック
npm run test     # ユニットテスト
```

## 生成テンプレート

### {{テンプレート名 1}}

```{{言語}}
{{テンプレートコード}}
```

### プレースホルダー一覧

| プレースホルダー | 説明 | 例 |
|----------------|------|------|
| `{{NAME}}` | {{説明}} | {{例}} |
| `{{TYPE}}` | {{説明}} | {{例}} |

## 失敗時の対応

| 状況 | 対応 |
|------|------|
| 型エラー | `npm run check` でエラー箇所を特定・修正 |
| CSS ルール違反 | `oklch()` に変換 |
| i18n キー欠落 | 両言語ファイルにキーを追加 |
| SSR エラー | ブラウザ API を `onMount` 内に移動 |

## 結果レポート

以下の形式で報告する:

```
## {{SKILL_NAME}} 結果
- 生成ファイル: {{パス}}
- 型チェック: ✅ / ❌
- CSS lint: ✅ / ❌
- i18n: ✅ / ❌
```
