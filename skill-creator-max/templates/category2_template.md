---
title: ビルド・デプロイ系スキルテンプレート
category: template
version: "1.0.0"
description: ビルド、CI/CD、デプロイ、環境構築用スキルのテンプレート
project: ffmpeg-command-generate-webapp
tags:
  - template
  - build
  - deploy
  - ci-cd
last_updated: "2025-02-20"
usage: >
  ビルド・デプロイ系のスキルを新規作成する際に使用する。
  このプロジェクトの /deploy パターンに準拠している。
  YAML フロントマターは削除すること。
examples:
  - deploy
---

# {{SKILL_NAME}}

{{デプロイ対象/ビルド対象}} の {{操作}} を実行し、結果を報告する。

## 前提チェック

実行前に以下を確認する:

1. {{前提条件 1}} — {{確認コマンドまたは方法}}
2. {{前提条件 2}} — {{確認コマンドまたは方法}}
3. {{前提条件 3}} — {{確認コマンドまたは方法}}

## 手順

### 1. {{フェーズ 1 のタイトル（例: ビルド）}}

```bash
{{実行コマンド}}
```

### 2. {{フェーズ 2 のタイトル（例: CI 完了待機）}}

```bash
{{実行コマンド}}
```

- {{成功時の条件}} → 次へ
- {{失敗時の条件}} → {{リカバリ手順}}

### 3. {{フェーズ 3 のタイトル（例: デプロイ確認）}}

```bash
{{確認コマンド}}
```

{{リトライ条件がある場合はここに記述}}

### 4. 結果レポート

以下の形式でユーザーに報告する:

```
✅ {{操作}} 完了
- Commit: <コミットハッシュ> <コミットメッセージ>
- CI:     成功（所要時間）
- URL:    <デプロイ URL>
```

## 失敗時の対応

| 状況 | 対応 |
|------|------|
| {{失敗パターン 1（例: CI 失敗）}} | {{対処: ログ確認→修正→再実行}} |
| {{失敗パターン 2（例: デプロイ失敗）}} | {{対処: 手動確認を促す}} |
| {{失敗パターン 3（例: URL 取得失敗）}} | {{対処: リトライまたはダッシュボード確認}} |

## 次のステップ（任意）

デプロイ完了後に実行を推奨するスキル:

- `{{/related-skill-1}}` — {{目的}}
- `{{/related-skill-2}}` — {{目的}}
